eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9097
spring:
  application:
    name: Employee-service
  datasource:
    url: jdbc:mysql://localhost:3306/project
    username: root
    password: vihas
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true